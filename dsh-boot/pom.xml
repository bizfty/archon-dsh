<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.bizfty.anchon</groupId>
        <artifactId>anchon</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>
    <artifactId>dsh-boot</artifactId>
    <packaging>jar</packaging>
    <name>Anchon :: DSH :: Boot</name>
    <description>Runnable application assembly: Spring Boot entry point + application.yml</description>
    <dependencies>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-util</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-tool</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-todo</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-plan</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-plan-task</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-fs</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-shell</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-skill</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-session</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-llm</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-agent</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-interaction</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-guard</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-compaction</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-subagent</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-context</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-jobs</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-mcp</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-sdk</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-credentials</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-sandbox</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-schedule</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-identity</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-feedback</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-storage</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-settings</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-lsp</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-code-runtime</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-user</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-goal</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-hooks</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-workflow</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-browser</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-github</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-postgres</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-mysql</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-search</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bizfty.anchon</groupId>
            <artifactId>dsh-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <!-- 数据库迁移：Liquibase starter（Spring Boot 4 起需 starter 才装配迁移执行；changelog 见 db/changelog/）。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-liquibase</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <!-- 启用资源过滤，使 application.yml 中的 @project.version@、@build.timestamp@ 被替换 -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>io.github.git-commit-id</groupId>
                <artifactId>git-commit-id-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- 测试与开发运行环境隔离：
                         dsh-boot 主资源含个人 profile 配置（application-local.yml / application-online.yml，
                         gitignore，通常由 shell 导出 SPRING_PROFILES_ACTIVE=local 激活），会覆盖
                         src/test/resources/application.yml 的 H2 测试数据源导致上下文启动失败。
                         这里在测试 fork JVM 中显式清空 spring.profiles.active（Java 系统属性优先级高于
                         OS 环境变量），保证测试始终使用 H2、可离线复现；需要特定 profile 的测试
                         请用 @ActiveProfiles 显式声明。 -->
                    <argLine>-Dspring.profiles.active=</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>